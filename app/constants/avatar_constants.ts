export const AVATAR_CONSTRAINTS = {
  MIN_SIZE: 200,
  MAX_SIZE: 5000,
  MAX_FILE_SIZE: '2mb',
  ALLOWED_EXTENSIONS: ['jpg', 'jpeg', 'png', 'webp'],
  OUTPUT_SIZE: 512,
  OUTPUT_QUALITY: 80,
} as const
